<properties>
    <service-loader initializableProblemHandler="ignore"/>
    <service-loader loadErrorHandler="WARN"/>

    <parsers>
        <parser class="org.apache.tika.parser.DefaultParser">
            <parser-exclude class="org.apache.tika.parser.ocr.TesseractOCRParser"/>
        </parser>
    </parsers>

    <!--  Tika leaves us guessing at how to filter for unwanted parsers.
       Exclude the parsers from the POM.  MATLab files we'll never encounter, for example.
       But filtering them out is challenging here.

    <detectors>
        <detector class="org.apache.tika.detect.DefaultDetector">
            <detector-exclude class="org.apache.tika.parser.mat.MatDetector"/>
        </detector>
    </detectors>
    <parsers>
        <parser class="org.apache.tika.parser.DefaultParser">
            <parser-exclude class="org.apache.tika.parser.mat.MatParser"/>
            <parser-exclude class="org.apache.tika.parser.ocr.TesseractOCRParser"/>
        </parser>
    </parsers>
    -->
</properties>
